{
  "name": "analytics-dashboard",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "docker:up": "docker compose -f infra/docker-compose.yml up -d",
    "docker:down": "docker compose -f infra/docker-compose.yml down",
    "docker:logs": "docker compose -f infra/docker-compose.yml logs -f",
    "dev": "echo 'Next.js web app has not been scaffolded yet.' && exit 1",
    "worker": "echo 'Worker has not been implemented yet.' && exit 1",
    "typecheck": "echo 'Typecheck has not been configured yet.' && exit 1"
  }
}
